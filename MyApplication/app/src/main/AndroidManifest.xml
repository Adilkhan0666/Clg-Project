<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/kec_logo_round"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/kec_logo_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AktuResult"></activity>
        <activity android:name=".Signup" />
        <activity android:name=".Signin" />
        <activity android:name=".SelectActivity" />
        <activity
            android:name=".CanteenNavigationActivity"
            android:label="@string/title_activity_canteen_navigation"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".UptuCircular" />
        <activity android:name=".AcademicUpdates" />
        <activity android:name=".PlacementUpdateActiviy" />
        <activity android:name=".NoticeActivity" />
        <activity android:name=".StudentPanel" />
        <activity android:name=".Enter_Student_Details" />
        <activity android:name=".AdminNoticePanelActivity" />
        <activity android:name=".LoginPage" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>